apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-stuff.fullname" . }}
  labels:
    {{- include "helm-stuff.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-stuff.selectorLabels" . | nindent 4 }}
